<UserControl x:Class="TrashTrackProjectV2.View.Pesan"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:TrashTrackProjectV2.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="600" 
             d:DesignWidth="622"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:PesanVM/>
    </UserControl.DataContext>

    <Grid>
        <Border Background="#F5F2EB" CornerRadius="0, 15, 15, 0">
            <StackPanel>
                <TextBlock Text="Pesan" FontWeight="Bold" FontSize="30" Height="41" Foreground="#1D1D21" Margin="60 15"/>
                <Image Height="228" Source="/img/maps.png" Stretch="Fill" Width="499"/>
                <Grid>
                    <Button Content="Pesan Tukang Sampah" Style="{StaticResource buttonMain2}" Margin="62,20,360,0" Width="200"/>
                    <TextBlock Text="Jatah Minggu Ini: " FontSize="18" FontWeight="Bold" Margin="393,15,80,30" Foreground="#1D1D21"/>
                    <TextBlock Text="{Binding DisplaySlotPaket}" Foreground="#1D1D21" FontSize="18" FontWeight="Bold" Margin="548,15,48,30" RenderTransformOrigin="0.5,0.5">
                        <TextBlock.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform/>
                                <SkewTransform/>
                                <RotateTransform Angle="0.113"/>
                                <TranslateTransform/>
                            </TransformGroup>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Grid>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
