<UserControl x:Class="TrashTrackProjectV2.View.Pesan"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:TrashTrackProjectV2.ViewModel"
             xmlns:wpf1="clr-namespace:Mapsui.UI.Wpf;assembly=Mapsui.UI.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="600" 
             d:DesignWidth="622"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:PesanVM/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.75*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#F5F2EB" CornerRadius="0, 15, 0, 0">
            <StackPanel>
                <TextBlock Text="Pesan" FontWeight="Bold" FontSize="30" Height="41" Foreground="#1D1D21" Margin="60 15"/>
                <wpf1:MapControl x:Name="MapControl" Height="228" Width="498" MouseRightButtonUp="MapRBUp" />
                <Grid>
                    <Button Content="Pesan Tukang Sampah" Style="{StaticResource buttonMain2}" Margin="62,20,360,0" Width="200"/>
                    <TextBlock Text="Voucher Tersisa: " FontSize="18" FontWeight="Bold" Margin="393,15,80,30" Foreground="#1D1D21"/>
                    <TextBlock Text="{Binding DisplayVoucher}" Foreground="#1D1D21" FontSize="18" FontWeight="Bold" Margin="548,15,48,30" RenderTransformOrigin="0.5,0.5"/>
                </Grid>
            </StackPanel>
        </Border>

        <Border Background="#F5F2EB" CornerRadius="0, 0, 15, 0" Grid.Row="1">
            <StackPanel>
                <TextBlock Text="OTW " FontWeight="Bold" FontSize="20"  Foreground="#1D1D21" Margin="60 10"/>
                <Border Background="#E4E1DC" Width="499" Height="131" CornerRadius="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="0.2*"/>
                            <ColumnDefinition Width="2.5*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!--// Baris 1 //-->
                        <TextBlock Text="Nama" FontSize="16" FontWeight="SemiBold" Foreground="#1D1D21"/>
                        <TextBlock Text=":" FontSize="16" FontWeight="SemiBold" Foreground="#1D1D21" Grid.Column="1"/>
                        <TextBlock Text="{Binding DisplayNama}" FontSize="16" FontWeight="SemiBold" Foreground="#1D1D21" Grid.Column="2"/>
                    </Grid>
                </Border>
            </StackPanel>

        </Border>
    </Grid>
</UserControl>
